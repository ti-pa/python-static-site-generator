from typing import List
from pathlib import Path

class Parser:
    extension = []
    extension = List[str]

    def valid_extension(extension):
        if is self.extension:
            return True
        else:
            return False
    
    def parse(path, source, dest):
        